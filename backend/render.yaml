services:
  - type: web
    name: leox-backend
    runtime: node
    buildCommand: npm install
    startCommand: node server-simple.js
    envVars:
      - key: PORT
        value: 3000
      - key: NODE_ENV
        value: production
