services:
  - type: web
    name: leox-backend
    runtime: node
    buildCommand: cd backend && npm install
    startCommand: cd backend && node server-simple.js
    envVars:
      - key: PORT
        value: 3000
      - key: NODE_ENV
        value: production
