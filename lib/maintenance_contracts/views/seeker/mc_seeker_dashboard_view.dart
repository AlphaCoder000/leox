import 'package:flutter/material.dart';

class McSeekerDashboardView extends StatelessWidget {
  const McSeekerDashboardView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Seeker Dashboard")),
      body: const Center(child: Text("Seeker Dashboard coming in Step 5")),
    );
  }
}
